import React from 'react';

//Контекgст
const AppContext = React.createContext({}); // оборачиваем объект в функцию реактКонтекст чтобы реакт знал если этот объект изменится

export default AppContext;
